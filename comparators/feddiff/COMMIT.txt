e7d109dc263d67435c9be8b856d1f13f973da094
# Pinned 2026-05-17 from https://github.com/mmendiet/FedDiff (main HEAD: "Initial commit", 2024-12-04).
#
# CAVEAT (issue 08, 2026-05-17):
#   Upstream repository currently contains only LICENSE + README.md placeholder.
#   The authors (Mendieta, Sun, Chen — WACV 2025) have not yet pushed the
#   FedDiff training/distillation code. The pin above is the upstream HEAD as
#   of the vendoring date so that future bumps are auditable.
#
#   NOTE: the issue brief originally cited `mendieta/FedDiff` (lowercase user)
#   which does not exist; the canonical upstream is `mmendiet/FedDiff`
#   (confirmed via the WACV 2025 PDF "Code is available at
#   https://github.com/mmendiet/FedDiff").
#
#   Consequences for the smoke wrapper in jobs/cfd_v2_comp_feddiff.sh:
#     * The wrapper targets `main.py` (the conventional entry-point) and
#       passes `--epsilon $4`. Both will only resolve once upstream publishes.
#     * Until then the wrapper will exit non-zero with a clear "upstream not
#       populated" message at the entry-point check step (no silent pass).
#     * DP accountant in the eventual code is Opacus per the WACV 2025 paper
#       (confirmed via search: "FedDiff ... employs the Opacus library in
#       PyTorch to track privacy budgets ... Poisson batch sampling").
#
#   Action when upstream populates:
#     1. cd comparators/feddiff && git pull && update SHA above.
#     2. Verify entry-point name (likely main.py) and the actual epsilon flag
#        name (argparse may use --epsilon, --target_epsilon, or --eps).
#     3. Update the `ENTRY` and `EPS_FLAG` variables in the wrapper.
#     4. Recheck the actual_epsilon / actual_delta extraction path.
