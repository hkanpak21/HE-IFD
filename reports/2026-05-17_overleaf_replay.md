# Overleaf replay — row-by-row diff (2026-05-17)

Generated by comparing `overleaf/*.tex` (current Overleaf snapshot) against `FL_TDSC/*.tex` (the corrected post-pivot state built across today's 19 commits). Lines are quoted as they currently sit in Overleaf; the "should be" column carries the post-pivot replacement, with a short rationale referring back to the relevant issue / discussion thread.

The Overleaf snapshot has already taken on several of today's changes (the abstract, the threat-model subsection in methodology, the malicious-clients future-work paragraph, the `atapoor2024vfhe` bib entry). What remains is the cascade of edits that follow from the linear-accumulator pivot and from the structural moves R3-3 / R3-6.

Two layers of edits below:

- **Surgical edits** — small, line-anchored substitutions (introduction §I-A enumeration, Contributions list, conclusion cross-references and numbers, bib entry).
- **Section-scale rewrites** — the post-Threat-Model body of `methodology.tex` (lines 32–296) and the experiments narrative both still describe the rejected block-wise protocol; they need wholesale replacement rather than line edits. The replacement source is cited per section.

---

## 1. `main.tex`

No surgical edits required. The active abstract (lines 55–62) and `\title` (line 37) already carry the post-pivot prose.

---

## 2. `introduction.tex`

### 2.1 §I-A "Our Approach" three-step enumeration (lines 14–21) — REPLACE

**Currently in Overleaf** (lines 14–21):

```latex
\subsection{Our Approach}
We present \textbf{HE-IFD} (\textbf{H}omomorphic \textbf{E}ncryption-\textbf{I}ntermediate \textbf{F}eature \textbf{D}istillation), a framework in which individual client teacher outputs are protected during knowledge transfer and all subsequent server-side computation. The protocol operates in three phases:

\begin{enumerate}
\item \textbf{Local training.} Each client trains a standard teacher model (e.g., ResNet-18) on their private data partition, starting from a shared random initialisation.
\item \textbf{Encrypted upload.} Each client runs its teacher model on its own data and records the inputs and outputs at each layer group boundary. We refer to these groups as \emph{blocks}. These input--output pairs tell the server what each block of the teacher produces, without revealing the raw data. Before encrypting, clients jointly agree on per-channel normalisation statistics so that features from different clients are on a comparable scale. The normalised pairs are then encrypted under Cheon--Kim--Kim--Song (CKKS) scheme and uploaded to the server in a single round; no further client interaction is needed.
\item \textbf{Server-side encrypted distillation.} The server trains a HE-compatible architecture in which non-polynomial operations such as ReLU and batch normalisation are replaced with polynomial equivalents that can be evaluated directly on ciphertexts (e.g., PolyResNet-18) for the student model on the server, block by block, using only encrypted inputs and targets. Student weights, activations, gradients, and loss values are all ciphertext throughout training.
\end{enumerate}
```

**Should be** (per issue 17 + issue 13; the C1–C4 paragraphs that already follow at lines 25–31 are correct, but this lead enumeration contradicts them by still describing the block-wise protocol):

```latex
\subsection{Our Approach}
We present \textbf{HE-IFD} (\textbf{H}omomorphic \textbf{E}ncryption-\textbf{I}ntermediate \textbf{F}eature \textbf{D}istillation), a framework in which individual client teacher outputs are protected during knowledge transfer and all subsequent server-side computation. The protocol operates in four phases:

\begin{enumerate}
\item \textbf{Local training.} Each client trains a standard teacher model (e.g., ResNet-18) on their private data partition.
\item \textbf{Encrypted upload of teacher logits on a public probe.} Each client evaluates its teacher on a public probe set $\mathcal{P}$ of $|\mathcal{P}|{=}5000$ samples and uploads the encrypted teacher logits $\langle T_i(\mathcal{P})\rangle$ to the server in a single round under a multiparty CKKS scheme with collectively held keys. No per-block intermediate features are uploaded.
\item \textbf{Server-side encrypted distillation against an aggregated ensemble target.} The server pools the encrypted logits into an ensemble target $\widetilde{Y}$ (depth-3 construction) and distils a student against it. The student weights are held in plaintext during training; the per-step encrypted state is a \emph{linear accumulator} $\langle\theta_E\rangle = \langle\theta_0^\star\rangle + \sum_t \eta\,\langle g_t\rangle$ over teacher-induced gradients applied to the plaintext student state.
\item \textbf{Collective key-switching and release.} The trained student is released to the clients via a single collective key-switching round that requires every client to participate. The server never observes any individual teacher output, student weight, or intermediate computation in plaintext.
\end{enumerate}
```

*Rationale.* The legacy enumeration is from the rejected block-wise protocol (per-block feature uploads + block-by-block server training). The C1–C4 paragraphs that follow at lines 25–31 are already aligned with the linear-accumulator pivot, so the enumeration directly above them is the lone hold-out making §I-A self-contradictory.

### 2.2 §I-A "Contributions" list (lines 38–49) — REPLACE

**Currently in Overleaf** (lines 38–49):

```latex
\subsection{Contributions}
Our contributions are as follows:

\begin{enumerate}
\item We propose, for the first time, progressive stacking for training HE-compatible student models fully under encryption: a block-by-block procedure with magnitude-regularised normalised MSE loss that achieves stable distillation of a full PolyResNet-18 student (11.17M parameters) from encrypted teacher features, where standard end-to-end distillation diverges.

\item We present a novel, fully encrypted one-shot distillation protocol in which clients upload encrypted per-block feature pairs in a single communication round and the server performs all distillation computation (forward pass, loss evaluation, backward pass, and weight update) entirely on ciphertexts, without exposing any plaintext data at any point during training.

\item We introduce client-side collaborative normalisation, a lightweight technique in which each client normalises its teacher outputs to zero mean and unit variance per channel before encryption, which eliminates magnitude incompatibility across heterogeneous teachers and prevents training divergence for $N \geq 4$ clients.

\item We provide a comprehensive empirical evaluation of HE-IFD across three architectures (ResNet-18, SimpleCNN, ViT-B/32), client counts $N \in \{1, 2, 4, 8, 16, 32\}$, and heterogeneity levels $\alpha \in \{0.1, 1.0\}$, including accuracy comparisons against differential privacy baselines, membership inference attack analysis, and communication and computation cost analysis.
\end{enumerate}
```

**Should be** (per issue 17's C1–C4 framing):

```latex
\subsection{Contributions}
Our contributions are as follows:

\begin{enumerate}
\item We introduce a one-shot encrypted Collaborative Federated Distillation protocol in which clients upload encrypted teacher logits on a single public probe (rather than per-block intermediate features) and the server distils a student against an aggregated encrypted ensemble target $\widetilde{Y}$. The construction reduces the per-client upload by two orders of magnitude relative to per-block feature uploads.

\item We introduce a \emph{linear-accumulator} encrypted SGD: the encrypted student state is a sum of teacher-induced gradient contributions applied to a plaintext student state, $\langle\theta_E\rangle = \langle\theta_0^\star\rangle + \sum_t \eta\,\langle g_t\rangle$, with per-step depth at most three multiplicative levels. The construction confines the entire protocol to a single CKKS level chain at $\log N \in \{14, 15\}$ with no bootstrapping.

\item We construct a division-free $\beta/\lambda$ ensemble boost: confidence reweighting is taken on plaintext scalars and applied as scalar-by-ciphertext multiplication, and the per-row variance is built as $\langle V_k\rangle = \sum_i w_i\langle T_{i,k}\rangle^2 - \langle\bar T_k\rangle^2$ at depth 1. The ensemble target is at most three multiplicative levels deep regardless of $N$.

\item We formalise a \emph{binding invariant} on threshold decryption (the only ciphertext ever decrypted is the final trained student $\theta_E$) and an all-zeros-amplification defence via DP-SGD teachers + per-row Gaussian noise, both standing without contradiction with the linear-accumulator construction.

\item We provide an empirical evaluation across MNIST, CIFAR-10, and the two extreme heterogeneity settings ($N{=}10$, $\alpha \in \{0.05, 0.3\}$), with accuracy comparisons against the strongest published one-shot baselines (Co-Boosting, FedMD), DP one-shot baselines (FedKT, FedDiff), and a membership-inference attack on the decrypted student.
\end{enumerate}
```

*Rationale.* The Overleaf contributions list still names "progressive stacking", "magnitude-regularised normalised MSE loss", "PolyResNet-18 student (11.17M parameters)", "block-by-block procedure", "client-side collaborative normalisation" — every one of those is an artefact of the rejected block-wise protocol and is grep-zero in our post-pivot methodology (issue 13). The $N \in \{1, 2, 4, 8, 16, 32\}$ / $\alpha \in \{0.1, 1.0\}$ grid is also obsolete; the resubmission's grid is $N{=}10$ with $\alpha \in \{0.05, 0.1, 0.3, 1.0\}$ per the PRD §7.2 + cover letter §3.

### 2.3 Missing §I-B "Motivation" subsection — INSERT (per issue 16, R3-3)

**Currently in Overleaf**: §I-A is followed directly by §I-A "Contributions" (the second subsection); there is no §I-B Motivation. The motivation paragraphs sit in `background.tex` §II-D ("Motivation for One-Shot Communication and Homomorphic Encryption", line 62) where R3-3 flagged them as too late.

**Should be**: insert the following subsection **between** §I-A "Our Approach" (ends at line 36) and §I-A "Contributions" (starts at line 38). Use the prose verbatim from `background.tex` lines 62–68; only the "as shown above" → `Section~\ref{sec:bg_server_inference}` re-aim is needed because the new placement is no longer adjacent to §II-B.

```latex
\subsection{Motivation}
\label{sec:intro_motivation}

One-shot federated learning~\cite{guha2019oneshot, wang2025towards} limits client--server communication to a single round, eliminating the iterative exposure that facilitates multi-round attacks. Knowledge distillation suits this setting naturally: clients train local teachers and transfer knowledge to a server-side student in a single step. However, data heterogeneity causes inconsistent teacher logits, and as shown in Section~\ref{sec:bg_server_inference}, even this single round of plaintext transfer remains exploitable by the server. Encrypting the transferred knowledge is therefore necessary, not optional.

Homomorphic encryption (HE)~\cite{cheon2017ckks} provides end-to-end cryptographic protection without injecting noise, avoiding the utility--privacy tradeoff of DP. However, applying HE to standard iterative training is structurally expensive. The systems reviewed in Section~\ref{sec:bg_he_fl} (POSEIDON, BatchCrypt, FedSHE) all encrypt per-round gradient updates and therefore exhaust the multiplicative-depth budget of CKKS~\cite{cheon2017ckks} (further discussed in Section~\ref{sec:ckks_prelim}) in every round; bootstrapping~\cite{cheon2018bootstrapping} refreshes the budget but at substantial latency. One-shot settings resolve this tension: ciphertexts are used once, and block-independent training with fresh ciphertexts confines the depth budget to a single block of the student rather than the full network. This principle was recently validated for encrypted MLP training~\cite{pirillo2025reboot}. HE-IFD extends it to federated one-shot distillation over heterogeneous clients.

DP partially addresses plaintext leakage but degrades the distillation signal. Applying local DP causes severe accuracy drops (up to $70\%$ on CIFAR-100 at $\epsilon = 0.1$~\cite{gad2024communication}), and one-shot settings consume the entire privacy budget immediately, precluding amplification across rounds. At meaningful privacy levels ($\epsilon \leq 1$), DP reduces FedMD to near random-guessing accuracy~\cite{sun2021fedmdnfdp}. HE avoids this tradeoff entirely.
```

*Rationale.* R3-3: "motivation belongs in §I, not §II-C". Lifting it to a dedicated §I-B subsection before "Our Approach" puts the *why* in front of the *what*, where the advisor explicitly asked.

---

## 3. `background.tex`

### 3.1 §II-D Motivation block (lines 62–68) — REMOVE

**Currently in Overleaf** (lines 62–68): the three motivation paragraphs (one-shot rationale + HE rationale + DP comparison).

**Should be**: remove these paragraphs entirely from `background.tex`; they live in §I-B per the insert above. Leave a breadcrumb comment so the move is auditable:

```latex
% Motivation paragraphs moved to introduction.tex section I-B per R3-3.
```

---

## 4. `methodology.tex` — SECTION-SCALE REWRITE (post-Threat-Model body)

The first 29 lines of `methodology.tex` (figure block + §framework lead + the full System and Threat Model subsubsection with `\label{sec:threat_binding}`) are already aligned with our work. **Everything from line 32 to the end of the file (line 296) still describes the rejected block-wise protocol and needs wholesale replacement.**

This affects:

- The figure caption at lines 4–9 (describes per-block feature uploads).
- Lines 40–42 "Overview of HE-IFD" (block-by-block server training).
- Algorithm 1 at lines 45–96 (HE-IFD with K+1 blocks, feature pairs $(\mathbf{x}_k, \mathbf{y}_k)$, MagReg loss).
- §Phase 1–4 at lines 98–171 (block boundaries, bridges, NormMSE, TrainableBridge).
- §Training Stability at lines 175–188 (TrainableBridge, MagReg, per-channel affine bridges).
- §Privacy Analysis at lines 192–197 (still says "per block").
- §Architecture-Specific Instantiations at lines 200–254 (PolyResNet-18, PolyBasicBlock, ChannelScale, MaxPool→Identity, "Block boundaries").
- §Communication and Computation Complexity at lines 258–296 (per-block depth budget, cascade refinement bootstrapping, 4.8 GB per client per block).

**Replacement source**: copy `FL_TDSC/methodology.tex` lines 32–228 verbatim. That source carries:

- A new `\par\noindent\textbf{Protocol overview.}` + binding-invariant paragraph + `Table~\ref{tab:cfd_phases}` (PRD §4.1 phase table: Phase / Who / What / HE cost, 7 rows).
- A new `\begin{algorithm}` "Encrypted CFD (one round)" `\label{alg:cfd}` — 20 `\STATE` lines covering DKG → key-switch with the linear accumulator at the centre.
- `\subsubsection{Phase~1..4}` with the new `\label{sec:phase1..4}`.
- `\paragraph{$\beta/\lambda$ secure handling}` `\label{sec:ensemble_target}` and `\paragraph{Linear-accumulator construction}` `\label{sec:linear_accumulator}` carrying the canonical equation $\langle\theta_E\rangle = \langle\theta_0^\star\rangle + \sum_t \eta\,\langle g_t\rangle$.
- `\subsection{Two-Stage Initialisation}` `\label{sec:twostage_init}` with A1/A2 ablations and a γ-variant remark.
- `\subsection{Communication Complexity}` `\label{sec:complexity}` + `Table~\ref{tab:comm_complexity}` (5 rows: client logit upload, client confidence upload, client synthetic upload (γ only), student download, key-switch shares). The concrete row at $|\mathcal{P}|{=}5000$, $C{=}10$, $N{=}10$, LeNet-5 ≈ 60k params: 4 ctxts logit + 1 ctxt confidence + 4 ctxts download + 1 share = ≈ 8 ctxts ≈ 8 MB per client per run.
- `\subsection{Privacy Analysis}` `\label{sec:privacy_proof}` reduced to a thin pointer back to `\ref{sec:threat_binding}` (the new threat-model subsection at the top of the file carries the full analysis).
- `\subsection{Architecture-Specific Instantiations}` `\label{sec:architecture}` with brief stubs for CNN and Transformer/ViT subsubsections (the operator-replacement framing now described as a *released-student inference-time compatibility surface only*, matching the R2-Q6 paragraph already in §Threat Model).

**New labels** introduced by the replacement that other files cross-reference and rely on: `sec:linear_accumulator`, `sec:ensemble_target`, `sec:twostage_init`, `sec:complexity`, `alg:cfd`, `tab:cfd_phases`, `tab:comm_complexity`, `eq:linear_accumulator`. All unique paper-wide.

**Figure caption (lines 4–9) — REPLACE in tandem**: the current caption still describes the four-phase block-wise protocol. Replace with a caption matching the new four-phase encrypted CFD: client teacher training → encrypted logit upload on public probe → server distils against encrypted ensemble target via linear accumulator → collective key-switch. The figure file `figures/HE-IFD_sysFigure.pdf` is itself stale (visualises the rejected protocol); the proper replacement is `figures/protocol_overview_v2.pdf`, regenerated from `figures/protocol_overview_v2.svg` via `rsvg-convert --format=pdf` (the hand-authored SVG was added under issue 12).

*Rationale.* The May-5 pivot established that the rejected block-wise HE-IFD is replaced by an encrypted CFD on a public probe, with the linear-accumulator construction confining per-step encrypted depth to ≤ 3 levels. Issue 13 mechanised the wholesale rewrite; this is the same content already in `FL_TDSC/methodology.tex`. The PRD's "Deprecation note for downstream builder LLMs" at lines 8–10 of `reports/2026-05-05_methodology_pivot.md` is the authoritative motivator.

---

## 5. `experiments.tex` — SECTION-SCALE REWRITE

The current §Experiments narrative is built around the rejected block-wise protocol: Phase 2a / Phase 2b cascade refinement with bootstrapping, per-block depth budget, the 460 GB total-upload number, PolyResNet-18 / SimpleCNN students with per-block feature uploads, "${\sim}$115 GB per client at $N{=}4$", "${\sim}$6.1 CPU-hours dominated by ${\sim}$900 bootstrapping operations". None of these numbers carry into the linear-accumulator + public-probe construction; they all need to be rewritten against the new protocol's communication and compute footprint.

This section is not yet committed in `FL_TDSC/experiments.tex` (it has the legacy content too; the issue tracker queues this work as issue 18 "A4.1 accuracy grid" + issue 19 "A4.2 communication table" + issue 20 "A4.3 time table"). Those issues are all gated on the QoS escalation (issue 03, HITL) because they require the actual headline grid to be executed.

In the meantime, the following surgical edits should land regardless of whether the headline grid is on the page yet:

### 5.1 Phase-description sentence (line 11) — REPHRASE

**Currently in Overleaf** (line 11, embedded in `\textbf{ResNet-18.}` paragraph):

```latex
Phase 2 trains each block independently on encrypted teacher features (80 epochs per block), then refines the cascade sequentially with bootstrapping to close the train-inference distribution gap.
```

**Should be**:

```latex
Phase 2 trains the student against an aggregated encrypted ensemble target $\widetilde{Y} = \sum_i \langle \alpha_i^\beta\rangle \cdot \langle T_i(\mathcal{P})\rangle$ on a public probe of $|\mathcal{P}|{=}5000$ samples; the per-step encrypted update is a linear accumulator $\langle\theta_E\rangle \mathrel{+}= \eta\,\langle g_t\rangle$ with depth $\leq 3$ per step, so the entire protocol fits inside a single CKKS level chain at $\log N \in \{14, 15\}$ with no bootstrapping.
```

*Rationale.* The Phase 2a/2b cascade-with-bootstrapping wording is the rejected protocol's. Under the linear-accumulator construction (issue 01 PRD patch + issue 13 methodology rewrite), bootstrapping is not used anywhere in the protocol.

### 5.2 §V-F "Future directions" paragraph (line 124) — REMOVE

**Currently in Overleaf** (line 124, last paragraph of `\subsection{Encrypted Arithmetic Feasibility}`):

```latex
\textbf{Future directions.} Recent work on stable polynomial network training~\cite{agamennone2025polynomial, alhossain2025training} suggests that improved activation initialisation may eliminate the need for cascade refinement, confining the entire protocol to the per-block depth budget. This would remove the bootstrapping requirement entirely and further optimize our framework.
```

**Should be**: remove from `experiments.tex`. The paragraph has already been moved into `conclusion.tex` (per issue 16, R3-6) — it is on Overleaf at `conclusion.tex` line 12 (and adjacent). The two copies need not to be both present. Leave a breadcrumb comment in `experiments.tex`:

```latex
% Future-directions paragraph moved to conclusion.tex per R3-6.
```

While leaving the moved paragraph in conclusion.tex, also retire its "cascade refinement" / "bootstrapping requirement" wording to match the linear-accumulator construction (see §6.3 below).

### 5.3 Communication-cost numbers (lines 83–85) — REPLACE

**Currently in Overleaf** (lines 83–85):

```latex
... In multi-round encrypted FL, each of 200 training rounds requires all $N$ clients to encrypt their 11.2M-parameter gradient updates as CKKS ciphertexts (${\sim}$2.7~GB per client per round), upload them, and receive the aggregated model back. This accumulates to ${\sim}1{,}068 \times N$~GB, reaching 4,272~GB at $N{=}4$ and 34~TB at $N{=}32$. Even unencrypted Vanilla FedAvg scales linearly with $N$, reaching 558~GB at $N{=}32$ over 200 rounds.

HE-IFD's total communication across all clients is approximately constant ($\sim460 GB$) regardless of $N$, because the total number of uploaded samples remains fixed. With $N$ clients, each client's local partition contains $50{,}000/N$ training samples, of which 10\% are contributed as encrypted features. The per-client upload therefore, decreases proportionally (${\sim}$115 GB at $N{=}4$, ${\sim}$15 GB at $N{=}32$), but the total across all clients remains fixed because the same training data is redistributed among more participants. At $N{=}32$, HE-IFD's total communication is lower than even unencrypted vanilla FL (460 vs.\ 558 GB), while providing cryptographic privacy.
```

**Should be** (rough sketch — final numbers land with issue 19 A4.2 table):

```latex
... In multi-round encrypted FL, each of 200 training rounds requires all $N$ clients to encrypt their 11.2M-parameter gradient updates as CKKS ciphertexts (${\sim}$2.7~GB per client per round), upload them, and receive the aggregated model back. This accumulates to ${\sim}1{,}068 \times N$~GB. Even unencrypted Vanilla FedAvg scales linearly with $N$, reaching hundreds of GB at large $N$ over 200 rounds.

HE-IFD's total communication is approximately $8 \times N$~MB regardless of the student size, because the upload carries only encrypted teacher logits on a public probe rather than per-block intermediate features. At $|\mathcal{P}|{=}5000$, $C{=}10$ classes, $N{=}10$ clients, packed into CKKS ciphertexts at $\log N{=}14$ (8192 slots), each client's logit upload is $\lceil 50000/8192\rceil = 4$ ciphertexts of $\sim$1~MB each, plus a single ciphertext for the per-client confidence scalar $\alpha_i$ and a 4-ciphertext encrypted-student download, for a total of $\approx 8$~MB per client per run. This is roughly four orders of magnitude smaller than the per-block-feature upload the rejected version of this paper proposed, and two orders smaller than one round of unencrypted FedAvg.
```

*Rationale.* The $\sim$115 GB / 460 GB numbers come from the per-block feature-upload protocol (every client uploads every intermediate feature map for 500 samples × every block boundary). Under the linear-accumulator + public-probe construction, the upload is a logit vector on a 5000-sample probe — orders of magnitude smaller. Concrete numbers must be reconciled against the A4.2 communication table once issue 19 runs.

### 5.4 Computation-cost numbers (line 98) — REPLACE

**Currently in Overleaf** (line 98):

```latex
... With 5000 training samples, SIMD packing reduces the effective number of batches to approximately one per epoch. Phase 2a (block-level distillation, no bootstrapping) completes in approximately 0.6 CPU-hours. Phase 2b (cascade refinement with bootstrapping) takes approximately 5.5 CPU-hours, dominated by ${\sim}$900 bootstrapping operations at 20 seconds each. The total server-side computation is ${\sim}$6.1 CPU-hours, independent of $N$ (Figure~\ref{fig:computation}).
```

**Should be** (rough sketch — final numbers land with issue 20 A4.3 table):

```latex
... With 5000 training samples, SIMD packing reduces the effective number of batches to approximately one per epoch. The full encrypted CFD protocol — DKG, encrypted ensemble-target construction (depth 3), $E_2$ encrypted SGD steps as linear accumulator (per-step depth $\leq 3$), and the final collective key-switch — completes in approximately X CPU-hours on a single core; no bootstrapping is required at any point in the protocol because the per-step depth stays well inside the seven-level chain available at $\log N{=}14$. The total server-side computation is independent of $N$ (Figure~\ref{fig:computation}).
```

Replace the placeholder "X" once the issue-04 TenSEAL smoke (`prototypes/cfd_tenseal_smoke.py`, job 1079768 in flight at the time of writing) and the issue-14 end-to-end single-cell run (gated on QoS) report wall-clock numbers. Until those land, the line currently in Overleaf overstates the encrypted compute cost by an order of magnitude or more (the rejected protocol's $\sim$900 bootstraps × 20 s do not happen in the new protocol).

### 5.5 §Encrypted Arithmetic Feasibility § "Multiplicative depth" line (line 122) — REPHRASE

**Currently in Overleaf** (line 122):

```latex
\textbf{Multiplicative depth.} During training, all parameters and data are ciphertexts. A single block training step requires approximately 12 levels, feasible with $\mathcal{N}{=}32768$ (18 levels at 128-bit security) without bootstrapping. Cascade refinement requires bootstrapping to refresh levels between blocks, with each bootstrap taking approximately 20 seconds per ciphertext.
```

**Should be**:

```latex
\textbf{Multiplicative depth.} Student weights are plaintext during training; only the per-step encrypted accumulator $\langle\Delta\rangle$ and the encrypted ensemble target $\widetilde{Y}$ are ciphertexts (Section~\ref{sec:linear_accumulator}). The per-step depth is bounded by 3 multiplicative levels (residual carry-over of $\langle\theta\rangle$, scalar plaintext-by-ciphertext for the learning rate, addition to the accumulator). The depth-3 ensemble-target construction (Section~\ref{sec:ensemble_target}) is incurred once per probe pass. The 7-level chain available at $\log\mathcal{N}{=}14$ absorbs both with margin; no bootstrapping is invoked anywhere in the protocol.
```

*Rationale.* The "12 levels per block step" + cascade-refinement-bootstrapping is the rejected protocol's depth audit. Under the linear-accumulator construction, the per-step depth is ≤ 3 and the entire protocol fits in a single CKKS level chain.

---

## 6. `conclusion.tex` — three surgical edits

### 6.1 Line 8: DP-SGD comparison numbers — REPHRASE

**Currently in Overleaf** (line 8):

```latex
Compared against differential privacy baselines, HE-IFD achieves competitive accuracy with a strictly stronger privacy guarantee: at $N$=4, $\alpha=0.1$, HE-IFD reaches 57.4\% while the best DP-SGD configuration (epsilon=10, weakest privacy) achieves 64.2\%, and tighter DP settings ($\varepsilon$ $\leq$ 1) collapse accuracy below 33\%.
```

**Should be** (the abstract's new headline numbers — May-5 working text — should anchor the conclusion too; issue 28 reconciles these against the actual A4.1 grid once it runs):

```latex
Compared against differential privacy baselines, HE-IFD achieves competitive accuracy with a strictly stronger privacy guarantee: at $N{=}10$, $\alpha{=}0.3$, the HE-IFD student reaches $0.965$ on MNIST against a mean individual teacher accuracy of $0.81$ ($+15.5$ pp) and $0.521$ on CIFAR-10 against a mean teacher of $0.408$ ($+11.3$ pp), with no central DP tax. Differential-privacy baselines at the same heterogeneity setting either collapse toward random-guess accuracy at the meaningful privacy regime ($\varepsilon \leq 1$) or pay a structural accuracy cost at the weakest privacy regime ($\varepsilon = 10$) that HE-IFD does not.
```

*Rationale.* The 57.4 / 64.2 / 33% numbers are from the rejected version's $N{=}4$, $\alpha{=}0.1$ block-wise experiments. The post-pivot resubmission's experimental grid is $N{=}10$, $\alpha \in \{0.05, 0.1, 0.3, 1.0\}$ (per the PRD §7.2). The May-5 working numbers in the abstract are the consistent ones to use here until the A4.1 grid lands.

### 6.2 Line 12: future-work upload-cost numbers — REPHRASE

**Currently in Overleaf** (line 12):

```latex
Future work along the present threat model includes reducing the one-shot upload cost (${\sim}$115\,GB per client at $N{=}4$, $\sim 460$ GB total across all clients) through ciphertext compression and feature selection, and investigating bootstrapping-free training schedules for deeper HE-compatible architectures.
```

**Should be**:

```latex
Future work along the present threat model includes reducing the encrypted-logit-upload constant factor through CKKS packing optimisations analogous to FedSHE's adaptive packing, extending the protocol to larger student architectures whose released-student inference under encryption stays inside the per-step depth budget, and the encrypted-synthetic-probe extension to settings without a public probe (the γ-variant of the methodology, conditional on the DP-DDPM profiling result).
```

*Rationale.* The "115 GB per client / 460 GB total" upload figures are the rejected per-block-feature-upload protocol's. Under the new public-probe protocol, the upload is $\approx 8$ MB per client per run (Section 5.3 above); upload-cost reduction is no longer the relevant compression target. The new compression target is the per-message CKKS packing constant. The "bootstrapping-free training schedules" item is also retired — the linear-accumulator construction is bootstrapping-free *by design*.

### 6.3 Line 14: binding-invariant cross-reference — FIX

**Currently in Overleaf** (line 14, inside the malicious-clients future-work paragraph):

```latex
... neither of which the binding invariant of Section~\ref{sec:bg_server_inference} addresses.
```

**Should be**:

```latex
... neither of which the binding invariant of Section~\ref{sec:threat_binding} addresses.
```

*Rationale.* `sec:bg_server_inference` is the §II-B inference-attacks-against-distillation background section, not the binding invariant. The binding invariant lives in the methodology's `\subsubsection{System and Threat Model}` which carries `\label{sec:threat_binding}` (introduced by issue 05). This is the same fix the orchestrator applied to `FL_TDSC/conclusion.tex` in the issue 05 follow-on commit.

---

## 7. `references.bib`

### 7.1 `zhang2022dense` entry (lines 95–101) — FIX METADATA

**Currently in Overleaf** (lines 95–101):

```latex
@inproceedings{zhang2022dense,
  title={Practical Data-Free Federated Learning},
  author={Zhang, Jie and Chen, Chen and Li, Bo and Lyu, Lingjuan and Wu, Shuang and Ding, Shouhong and Shen, Chunhua and Qi, Chao},
  booktitle={NeurIPS},
  volume={34},
  pages={11919--11932},
  year={2021}
}
```

**Should be** (per the references audit in `FL_TDSC/CHANGES.md` §6 finding 1):

```latex
@inproceedings{zhang2022dense,
  title={{DENSE}: Data-Free One-Shot Federated Learning},
  author={Zhang, Jie and Chen, Chen and Li, Bo and Lyu, Lingjuan and Wu, Shuang and Ding, Shouhong and Shen, Chunhua and Qi, Chao},
  booktitle={NeurIPS},
  volume={35},
  pages={21414--21428},
  year={2022},
  url={https://arxiv.org/abs/2112.12371}
}
```

*Rationale.* The current entry carries an early-draft title, wrong year (2021 vs 2022), and wrong NeurIPS volume (34 vs 35). The DENSE paper at arXiv:2112.12371 was published at NeurIPS 2022 (volume 35) under the canonical title above. Likely an early-draft title kept after camera-ready renaming. Author list matches, so this is a metadata fix not a paper swap.

---

## Order of application (suggested)

If you apply these in batches the manuscript will compile cleanly at each step. Suggested order so each compile checkpoint passes:

1. **References fix** — `references.bib` zhang2022dense entry. Risk-free; bibtex re-run produces a clean log.
2. **Conclusion cross-reference + numbers** — three small edits to `conclusion.tex` (§6.1, §6.2, §6.3 above). The xref fix is independent of the rewrite cascade; the numbers should reconcile against the abstract that is already in place.
3. **Introduction §I-A enumeration + Contributions** — replace lines 14–21 and lines 38–49 of `introduction.tex` (§2.1 + §2.2 above). After this, §I-A is internally consistent (lead enumeration matches the C1–C4 paragraphs that follow).
4. **Introduction §I-B Motivation insert + background.tex removal** — insert the new §I-B Motivation subsection in `introduction.tex` (§2.3 above) and remove the source paragraphs from `background.tex` lines 62–68 (§3.1 above). After this, motivation reads in §I as R3-3 requested.
5. **Methodology wholesale replacement** — copy `FL_TDSC/methodology.tex` lines 32–228 into Overleaf `methodology.tex` lines 32–296 (§4 above). Update the Figure 1 caption + swap the figure file from `HE-IFD_sysFigure.pdf` to `protocol_overview_v2.pdf` in the same pass.
6. **Experiments surgical edits** — lines 11, 122, 124 (§5.1, §5.5, §5.2 above). The full headline-grid + communication / time tables lands later via issues 18–20 once QoS escalation (issue 03) unblocks compute.
7. **Experiments communication + computation numbers** — §5.3 + §5.4 above, with placeholder X for the wall-clock until the issue-04 smoke + issue-14 single-cell run report concrete numbers.

After step 5, the paper's narrative is fully aligned with the linear-accumulator / public-probe pivot end-to-end. Steps 6 and 7 are the experiments-side rewrites that depend on QoS; they can land in a follow-up pass once the headline numbers exist.

---

## Cross-reference — what's in `FL_TDSC/CHANGES.md`

Every edit above already has a corresponding before/after entry in `FL_TDSC/CHANGES.md` (the Overleaf replay log). Specifically:

- §6 of CHANGES.md — references audit (covers §7.1).
- §7 — PRD staleness patches (background context for the methodology rewrite).
- §8 — A9 future-work paragraph on malicious clients (covers §6.3 above by virtue of the issue-05 follow-on).
- §9 — A8 threat-model rewrite (covers the post-pivot Threat Model already in Overleaf and the new `\label{sec:threat_binding}` that §6.3 fixes against).
- §10 — A11 threat-model SVG.
- §11 — A11 protocol-overview SVG (the figure that should replace `HE-IFD_sysFigure.pdf` in §4 above).
- §12 — A10 abstract + §I-A "Our Approach" rewrite (covers §2.1, §2.2, and is the source of the abstract that is already in Overleaf).
- §13 — A1 wholesale methodology rewrite (covers §4 above).
- §14 — A11 motivation move + future-directions move (covers §2.3, §3.1, §5.2 above).

Read the CHANGES.md entry for the line-level "Before" if you want the verbatim pre-edit text to copy back into Overleaf revision history for any of these edits.
