# heifd_cnn5_cifar10_headline

Issue 014 Sub-task C — Phase II from-scratch headline grid for CNN-5 on CIFAR-10, the third of the three from-scratch datasets in the headline matrix (MNIST/MLP landed in M1; LeNet/FMNIST in Sub-task A). 450 cells = 1 backbone × 5 N values {1, 5, 10, 20, 50} × 5 α values {0.01, 0.05, 0.1, 0.3, 1.0} × 6 methods {no_phase0, warmup_only_labelled, labelled_probe_warmup, raw_union_K20, dp_avg_eps2_K20, dp_avg_eps8_K20} × 3 seeds {42, 43, 44}; K=300 (from-scratch legacy default), Round-2.5 backbone hparams baked into `BACKBONES["cnn5_cifar10"]` (teacher_epochs=60, teacher_lr=0.01 cosine, oracle_epochs=100, warmup_epochs=10). Wrapper `jobs/heifd_014_cnn5_cifar10.sh` is queued but BLOCKED — the orchestrator submits it only after the Round-2.5 CNN-5 verify clears both gates (IID raw_union α=1.0 ≥ 0.60 AND m4_ood α=0.05 ≥ 0.40); placeholder README, auto-overwritten by `src.report.write_report` once the first sweep cell lands.
