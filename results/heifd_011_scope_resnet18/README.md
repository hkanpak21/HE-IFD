# heifd_011_scope_resnet18

Trainable-layer-scope focused comparison on the resnet18/CIFAR-10/α=0.05/N=10 regime that issues 008/010/013 jointly localised, testing whether adding capacity beyond the linear head (LoRA adapter on the head, or a 2-layer MLP head simulating a "last block") closes the ~2.2pp residual that issue 010's Round-1 best KD hparams (K=100, τ=1, lr=0.001 → 0.7617) left vs the warmed θ₀≈0.74. Sweeps `trainable_scope ∈ {head_only, lora_8, last_block}` × `method ∈ {no_phase0, raw_union_K20}` × `seeds {42, 43, 44}` = 18 cells, all using the issue-010 best KD hparams so scope (capacity) is isolated from KD hparams. The auto-writer populates the table below; the orchestrator appends a methodology-impact verdict post-run for the issue-011 HITL touchpoint (the framing decision: does "tiny head suffices" shift to "small LoRA adapter suffices"?).
