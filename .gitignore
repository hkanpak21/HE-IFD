# LaTeX build artefacts — paper dir (PDFs and figures stay tracked)
docs/paper/*.aux
docs/paper/*.log
docs/paper/*.out
docs/paper/*.toc
docs/paper/*.bbl
docs/paper/*.blg
docs/paper/*.fdb_latexmk
docs/paper/*.fls
docs/paper/*.synctex.gz

# LaTeX build artefacts (scoped to FL_TDSC/ so they don't mask slurm .out files)
FL_TDSC/*.aux
FL_TDSC/*.log
FL_TDSC/*.toc
FL_TDSC/*.bbl
FL_TDSC/*.blg
FL_TDSC/*.out
FL_TDSC/*.fdb_latexmk
FL_TDSC/*.fls
FL_TDSC/*.synctex.gz
FL_TDSC/*.lof
FL_TDSC/*.lot
FL_TDSC/*.nav
FL_TDSC/*.snm
git_token.txt
# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/

tools/


# Conda / venv
.conda/
venv/
.venv/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Slurm scratch from the v1 sweep (the per-case runs/ dir keeps them).
slurm-*.out
*.sbatch.out

# Datasets live under data/. Don't track them; they're large and
# reproducible by torchvision download or by copying from the user's cache.
data/MNIST/
data/FashionMNIST/
data/cifar-10-batches-py/
data/cifar-10-python.tar.gz
data/mnist/

# Teacher cache. Reproducible by re-running training (we keep the seed),
# but heavy and not worth tracking.
cache/

# MIA suite (issue 021) per-model shadow checkpoints. Each is the IN-mask + φ /
# loss / surrogate-φ vectors over the attack pool for one target/shadow model;
# regenerable from the (seed-keyed) protocol run, heavy in aggregate. The scored
# per-cell JSON + summary.json + README (the actual results) ARE tracked.
results/heifd_021_mia/shadows/

# Secrets — NEVER commit (the repo is public). HF tokens, API keys, etc.
.env
.env.*
*.env
secrets.*

# Local-only Claude state
.claude/
git_token.txt

FL_TDSC/archive/

# Heavy/binary artifacts — track the notebook, CSVs, scripts, and text instead,
# so a context-zero agent has readable source without binary bloat.
*.xlsx
results/colab_results/bundle/figures/
REJECTED_PAPER/*.pdf
.gstack/

# Keep the one-page change summary (source + pdf) tracked, unlike the big rejected PDFs
!REJECTED_PAPER/changes_summary.pdf
