# Slop pass, find and paste

Written 2026-08-23 after the no-ai-slop and Kupcu-deck check.

**Sequencing matters here.** Overleaf still holds the pre-restructure paper.
These 21 sentence changes sit on top of the restructured local files, so they
reach Overleaf inside the whole-file replacement you chose, not before it. Use
this file to see exactly what changed and to check the replacement landed, and
as a find-and-paste list only if you want to apply them to a copy that already
has the restructure.

Every block is one sentence. FIND the left text, PASTE the right text. Each
FIND string occurs exactly once in its file.

## 1. `figures/serving.tex`

FIND
```
\caption{Answering one query. The querying client computes the features with its own backbone and adapter and encrypts them, so the serving party never sees the feature vector and cannot invert it to recover the query. The serving party holds the evaluation keys only: it applies the encrypted head, obtains encrypted logits, and reduces them to the index of the largest entry by a tournament of homomorphic comparisons. A quorum of clients then switches that index to the querying client's key, so the other clients contribute shares without learning the answer.}
```

PASTE
```
\caption{Answering one query. The querying client computes the features with its own backbone and adapter and encrypts them, so the server never sees the feature vector and cannot invert it to recover the query. The server holds the evaluation keys only. It applies the encrypted head, obtains encrypted logits, and reduces them to the index of the largest entry by a tournament of homomorphic comparisons. A quorum of clients then switches that index to the querying client's key, so the other clients contribute shares without learning the answer.}
```

## 2. `sections/experiments.tex`

FIND
```
The trainable unit is a rank-$8$ low-rank adapter with its down-projection frozen at a shared public initialisation, together with a classifier head. No labelled public data is used anywhere.
```

PASTE
```
The trainable unit is a rank-$8$ low-rank adapter with its down-projection frozen at a shared public initialisation, together with a classifier head. We use no labelled public data anywhere.
```

## 3. `sections/experiments.tex`

FIND
```
caps and are used whole. Data are partitioned across clients by a Dirichlet distribution over
```

PASTE
```
caps, and we use those whole. Data are partitioned across clients by a Dirichlet distribution over
```

## 4. `sections/experiments.tex`

FIND
```
\tronly{The more informative reading concerns which arrangement is selected. The estimator
```

PASTE
```
\tronly{The more informative reading concerns which arrangement the estimator selects. The estimator
```

## 5. `sections/experiments.tex`

FIND
```
backbone, because both of its terms are measured on the same backbone. At $\Nc=5$
```

PASTE
```
backbone, because we measure both of its terms on the same backbone. At $\Nc=5$
```

## 6. `sections/experiments.tex`

FIND
```
seen, cannot be measured locally at all. The procedure compares an accurate number
```

PASTE
```
seen, the client cannot measure locally at all. The procedure compares an accurate number
```

## 7. `sections/experiments.tex`

FIND
```
We do not simulate the cryptography. The protocol is implemented in real
```

PASTE
```
We do not simulate the cryptography. We implement the protocol in real
```

## 8. `sections/experiments.tex`

FIND
```
of \cref{sec:intro}: no intermediate training artifact is ever exposed. It is not
```

PASTE
```
of \cref{sec:intro}. No intermediate training artifact is ever exposed. It is not
```

## 9. `sections/experiments.tex`

FIND
```
$2.2\times10^4$ on Banking77. The binding case is the small label space, which is
worth stating plainly because it runs against intuition. a head with few rows is cheap to copy. It is also the case in which the shared
```

PASTE
```
$2.2\times10^4$ on Banking77. The binding case is the small label space. A head with few rows is cheap to copy. It is also the case in which the shared
```

## 10. `sections/method.tex`

FIND
```
protocol is \emph{one-shot}: each client uploads once. (2) Confidentiality is \emph{cryptographic}. A client's contribution is never
```

PASTE
```
protocol is \emph{one-shot}. Each client uploads once. (2) Confidentiality is \emph{cryptographic}. A client's contribution is never
```

## 11. `sections/method.tex`

FIND
```
\emph{never disclosed}: no party, client or server, ever holds the trained
```

PASTE
```
\emph{never disclosed}. No party, client or server, ever holds the trained
```

## 12. `sections/method.tex`

FIND
```
weights. Every nonlinearity of the network would then have to be evaluated
```

PASTE
```
weights. The client would then have to evaluate every nonlinearity of the network
```

## 13. `sections/method.tex`

FIND
```
\Cref{alg:serve} states it.

\paragraph{Encryption of the query} The server could be given the
```

PASTE
```
\Cref{alg:serve} states it.

\paragraph{Encryption of the query} The client could send the
```

## 14. `sections/method.tex`

FIND
```
\tronly{\paragraph{Encryption of the query} The server could be given the
```

PASTE
```
\tronly{\paragraph{Encryption of the query} The client could send the
```

## 15. `sections/method.tex`

FIND
```
protocol exists to protect. The price is that the head is applied ciphertext by
```

PASTE
```
protocol exists to protect. The price is that the server applies the head ciphertext by
```

## 16. `sections/method.tex`

FIND
```
extraction impossible, and \cref{sec:threat} states the resulting bound honestly,
```

PASTE
```
extraction impossible, and \cref{sec:threat} states the resulting bound
```

## 17. `sections/prelim.tex`

FIND
```
decryption requires the cooperation of a quorum of clients. Two further protocols of the same family are
used below. The first is a key switch to a designated public key, which re-encrypts
```

PASTE
```
decryption requires the cooperation of a quorum of clients. We use two further protocols of the same family
below. The first is a key switch to a designated public key, which re-encrypts
```

## 18. `sections/related.tex`

FIND
```
revealed per-round sums have themselves been shown to leak across
rounds~\cite{kerkouche2023client}.
```

PASTE
```
Kerkouche et al. show that the revealed per-round sums themselves leak across
rounds~\cite{kerkouche2023client}.
```

## 19. `sections/related.tex`

FIND
```
revealed per-round sums have themselves been shown to leak across
rounds~\cite{so2023securing,kerkouche2023client}.
```

PASTE
```
Kerkouche et al. and So et al. show that the revealed per-round sums themselves leak across
rounds~\cite{so2023securing,kerkouche2023client}.
```

## 20. `sections/related.tex`

FIND
```
polynomial approximations so that they can be evaluated
```

PASTE
```
polynomial approximations so that the server can evaluate them
```

## 21. `sections/security.tex`

FIND
```
\Cref{func:ideal} defines the ideal functionality $\Fhe$ against which the
protocol is measured. The server is an entity outside $\Fhe$, so it may be corrupt. The form of the functionality follows the
```

PASTE
```
\Cref{func:ideal} defines the ideal functionality $\Fhe$ against which we
measure the protocol. The server is an entity outside $\Fhe$, so it may be corrupt. The form of the functionality follows the
```

## 22. `sections/security.tex`

FIND
```
\tronly{\Cref{thm:malicious} keeps the server honest. That assumption is necessary,
and the reason is worth stating, because it marks the boundary of what this
message pattern can achieve.}
```

PASTE
```
\tronly{\Cref{thm:malicious} keeps the server honest. That assumption is necessary.}
```

