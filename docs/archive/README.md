# Archive

Superseded documents, kept for provenance. Nothing here describes the current
method. Do not mine these for methodology, equations, or the threat model.

- `notes/` — records from before the encrypted-serving design was settled,
  including the pre-fine-tuning-pivot checkpoint and the paper outline that
  assumed the model would be released.
- `issues/` — task briefs whose work has landed, and briefs written against the
  earlier distillation method.

The current plan is `docs/plan/paper-rewrite.md`.
